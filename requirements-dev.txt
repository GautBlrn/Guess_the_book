# =============================================================================
# Dependances de developpement : pipeline complet, notebooks, tests.
# =============================================================================
#
# PYTHON 3.12 OBLIGATOIRE. C'est la version du Dockerfile (python:3.12-slim),
# et le plafond de cette stack : gensim et spacy ne publient pas de wheel
# au-dela de cp313, tokenizers encore moins. Sur une version plus recente pip
# retombe sur une compilation depuis les sources, qui echoue (le C genere par
# Cython accede a PyLongObject.ob_digit, champ deplace par CPython 3.12).
#
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#
# Sur Ubuntu 26.04, python3.12 n'est pas dans les depots : passer par
# ppa:deadsnakes/ppa (cf. README).

# Pipeline + IO
boto3>=1.34
botocore>=1.34
python-dotenv>=1.0
requests>=2.31
pyarrow>=15.0
pandas>=2.1
numpy>=1.26
scipy>=1.11

# NLP
spacy>=3.7
gensim>=4.3
scikit-learn>=1.4

# Reduction de dimension / visualisation
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18
umap-learn>=0.5

# Notebooks
jupyter>=1.0
nbformat>=5.9
ipykernel>=6.27

# Tests
# rouge-score et nltk servent d'implementations de reference dans
# tests/test_evaluation.py : nos metriques faites main sont comparees aux
# leurs. Ils ne sont jamais importes par le code de pipeline/.
pytest>=8.0
rouge-score>=0.1.2
nltk>=3.8

# App
streamlit>=1.30
altair>=5.2

# Resume abstractif (BARThez) et embeddings de phrases (CamemBERT)
# Versions epinglees : transformers >= 4.50 a casse le tokenizer slow de BARThez,
# qui ne dispose pas de version fast. On reste sur la 4.41 qui fonctionne.
transformers==4.41.2
tokenizers==0.19.1
torch>=2.1
sentencepiece>=0.2.0
sentence-transformers>=2.3